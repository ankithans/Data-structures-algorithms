#include <bits/stdc++.h>
using namespace std;


void inputOutput() {
#ifndef ONLINE_JUDGE
	freopen("../input.txt", "r", stdin);
	freopen("../output.txt", "w", stdout);
#endif
}


int main() {
	inputOutput();

	cout << "abc";


	return 0;

}

/*
	There are 6 bitwise operators in c++
		1. AND	&
		2. OR 	|
		3. XOR	 ^
		4. Left Shift operator <<
				if we assume that the leading y bits are 0, then the
				result of (x << y) is equivalent to x * 2^y

		5. right shit operator >>
				(x >> y) is equivalent to x / 2^y

		6. Bitwise NOT ~


		odd - 1st bit is 0
		even 1st bit is 1
		if(x&1)

*/


